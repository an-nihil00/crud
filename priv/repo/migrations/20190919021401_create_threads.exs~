defmodule Chan.Repo.Migrations.CreateThreads do
  use Ecto.Migration

  def change do
    create table(:threads) do
      add :board, :string
      add :subject, :string
      add :op, :integer
      add :replies, {:array, :integer}

      timestamps()
    end

  end
end
