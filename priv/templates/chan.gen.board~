defmodule Chan.Repo.Migrations.Create<%= String.capitalize(@abb)%>Board do
  use Ecto.Migration

  def change do
    create table(:<%= @abb%>_threads) do
      add :subject, :string
      add :op, :integer
      add :posts, {:array, :integer}

      timestamps()
    end
    create table(:<%= @abb%>_posts) do
      add :name, :string
      add :comment, :text
      add :image, :string

      timestamps()
    end
    execute "INSERT INTO boards (abb, name, posts) VALUES ('<%= @abb%>','<%= @name%>',0)"
  end
end